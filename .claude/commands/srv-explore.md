---
description: Спросить readonly-эксплорер сервера (remote MCP, только чтение) по свободному вопросу
---

Запусти readonly-исследование сервера по вопросу: **$ARGUMENTS**

Следуй скиллу `srv-explore`. Порядок:

1. Убедись, что подключён remote MCP `srv-explore` (инструмент `srv_explore`
   доступен). Нет — попроси инженера подключить его (туннель + `claude mcp add`,
   см. `srv_explore/README.md`), токен выдаёт админ. Сам токен/адрес не выдумывай.
2. Вызови инструмент **`srv_explore`** с этим вопросом. Он вернёт `job_id`.
3. Поллифай **`srv_explore_status`** по `job_id`, пока `status` не станет `done`
   (или `error`). Не крути слишком часто.
4. Верни инженеру findings-отчёт из результата (Вопрос → Что проверил → Находки →
   Гипотеза → Предложенное действие).

Агент на сервере readonly по построению (guard + readonly-роль БД + dontAsk) и ничего
не меняет. Если находка требует изменения — в отчёте будет точное предложенное
действие. Выполнить его можешь ты (основной агент) — но **только после явного
одобрения человеком** и его доступом, живьём в сессии, не через PR. Без одобрения
ничего на сервере не меняй.
