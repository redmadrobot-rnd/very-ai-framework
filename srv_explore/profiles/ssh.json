{
  "plugin": "ssh",
  "commands": ["ssh"],
  "desc": "ssh (удалённая команда рекурсивно через гард)",
  "handler": "ssh",
  "note": "Allowlist безопасных флагов (без -o/-F/-L/-R/-D/-W/-J — ProxyCommand/туннели = локальное исполнение). Удалённая команда рекурсивно проверяется тем же гардом. Удалённый хост вне нашей песочницы — поэтому ssh держится на гарде, а не на ядре."
}
